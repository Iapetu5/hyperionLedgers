/** Alias for Iapetus `company-pickup` imports — same handoff as add-company. */

export {
  ADD_COMPANY_KEY,
  ADD_COMPANY_RETURNS,
  addCompanyHref,
  clearSelectedCompany,
  readSelectedCompany,
  safeAddCompanyReturn,
  saveSelectedCompany,
  type SelectedCompany,
} from "@/lib/add-company";

"use client";

import { TryDemoLink } from "@/components/marketing/TryDemoCta";

export function TryPageDemoLink({ className }: { className?: string }) {
  return <TryDemoLink className={className} />;
}
